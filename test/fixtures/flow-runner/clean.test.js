// Fixture for test/unit/flow-runner.test.js: the baseline a complete run looks like.
import test from 'brittle'

test('first', async (t) => { t.pass('first') })
test('second', async (t) => { t.pass('second') })
